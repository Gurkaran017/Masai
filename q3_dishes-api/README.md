# Simple Dishes API (CRUD with Express and db.json)

## Features
- Add, retrieve, update, and delete dish entries.
- Search by (partial) name using query params.
- Handles undefined routes and missing data.

## Setup

```bash
npm install
node server.js
